<?php

use Illuminate\Database\Seeder;
use App\Role;
use App\User;

class RoleSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
      // $role = new Role();
      // $role->name = "admin";
      // $role->display_name = "Admin";
      // $role->description = "Big Boss";
      // $role->save();
      // $user = User::find(1);
      // $user->attachRole($role);
      // $user->save();
    }
}
